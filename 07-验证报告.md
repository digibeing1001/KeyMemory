# KeyMemory 认证与正常使用体验验证报告

日期：2026-08-06

## 审查范围与验收标准

- 首次打开时可顺利注册主账户，密码框不会再被错误表达为查找旧密码。
- 注册后可登出、使用正确密码重新登录，错误密码有清晰反馈。
- 审查本次认证链路暴露的相似正常使用体验问题。
- 通过仓库验证与发布检查后推送 GitHub。

## 规格符合度

结论：通过，无未修复发现。

- 新安装通过 `/api/auth/status` 自动进入注册；已有账户只显示登录，避免进入必然被拒绝的主账户注册。
- 注册使用 `new-password`，登录使用 `current-password`；加入确认密码、显示密码、最少 8 位和中英文错误反馈。
- 邮箱在服务端统一去空格并转小写，登录查询不区分大小写。
- Playwright 已完成注册、密码不一致、首次说明、手机侧栏、登出、错误密码、正确密码重新登录的完整旅程。

## 工程质量

结论：通过，无未修复发现。

- 新增认证路由回归，覆盖首次状态、弱密码、输入规范化、登录、登出与 token 撤销。
- 通用请求封装只在实际有 body 时声明 JSON，修复登出、邮箱同步、Dream、回收站恢复等空 POST 被 Fastify 拒绝的问题。
- 移动端侧栏显式覆盖桌面内联层级，确保侧栏位于遮罩之上。
- 恢复“项目整理”菜单对应的页面渲染，并补全所有页面模式的本地偏好恢复。

## 运行风险

结论：通过，无阻断项。

- 注册内部异常不再直接泄露数据库错误；重复邮箱返回稳定错误码。
- 登出已验证服务端 session 真正撤销，而非仅清除浏览器状态。
- 浏览器验收无意外控制台错误、无意外 4xx/5xx、无无障碍名称缺失；CLS 0.019，LCP 约 1.37 秒。
- 未覆盖：不同浏览器自带密码管理器的具体弹层文案；已通过标准 autocomplete 语义消除错误触发条件。

## 验证证据

- `node --import tsx --test src/core/__tests__/auth.test.ts`：2/2 通过。
- `node scripts/verify.mjs`：通过（类型检查、生产构建、22/22 记忆评测、检索、MCP、Loop、邮箱、LLM、自动化、L3 审计）。
- 隔离内存数据库 Playwright QA：通过，桌面 1440、平板 768、手机 375 三种视口均已截图检查。
- `pnpm --config.verify-deps-before-run=false release:check`：通过。
