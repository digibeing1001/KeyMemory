# KeyMemory 认证与正常使用体验验证报告

日期：2026-08-06

## 审查范围与验收标准

- 首次打开时可顺利注册主账户，密码框不会再被错误表达为查找旧密码。
- 注册后可登出、使用正确密码重新登录，错误密码有清晰反馈。
- 审查本次认证链路暴露的相似正常使用体验问题。
- 通过仓库验证与发布检查后推送 GitHub。

## 规格符合度

结论：通过，无未修复发现。

- 新安装通过 `/api/auth/status` 自动进入注册；已有账户只显示登录，避免进入必然被拒绝的主账户注册。
- 注册使用 `new-password`，登录使用 `current-password`；加入确认密码、显示密码、最少 8 位和中英文错误反馈。
- 邮箱在服务端统一去空格并转小写，登录查询不区分大小写。
- Playwright 已完成注册、密码不一致、首次说明、手机侧栏、登出、错误密码、正确密码重新登录的完整旅程。

## 工程质量

结论：通过，无未修复发现。

- 新增认证路由回归，覆盖首次状态、弱密码、输入规范化、登录、登出与 token 撤销。
- 通用请求封装只在实际有 body 时声明 JSON，修复登出、邮箱同步、Dream、回收站恢复等空 POST 被 Fastify 拒绝的问题。
- 移动端侧栏显式覆盖桌面内联层级，确保侧栏位于遮罩之上。
- 恢复“项目整理”菜单对应的页面渲染，并补全所有页面模式的本地偏好恢复。

## 运行风险

结论：通过，无阻断项。

- 注册内部异常不再直接泄露数据库错误；重复邮箱返回稳定错误码。
- 登出已验证服务端 session 真正撤销，而非仅清除浏览器状态。
- 浏览器验收无意外控制台错误、无意外 4xx/5xx、无无障碍名称缺失；CLS 0.019，LCP 约 1.37 秒。
- 未覆盖：不同浏览器自带密码管理器的具体弹层文案；已通过标准 autocomplete 语义消除错误触发条件。

## 验证证据

- `node --import tsx --test src/core/__tests__/auth.test.ts`：2/2 通过。
- `node scripts/verify.mjs`：通过（类型检查、生产构建、22/22 记忆评测、检索、MCP、Loop、邮箱、LLM、自动化、L3 审计）。
- 隔离内存数据库 Playwright QA：通过，桌面 1440、平板 768、手机 375 三种视口均已截图检查。
- `pnpm --config.verify-deps-before-run=false release:check`：通过。

---

# 记忆邮箱协作中转与主题归档验证报告

日期：2026-08-06

## 审查范围与验收标准

- 已发现 Agent 使用竖排 Logo、名称和接入状态，图像不拉伸、不压缩。
- Agent 开工前读取项目主题和相关记忆，并把整理后的上下文简报发回同一主题。
- 归档时把完整主题整理为唯一、可更新的长期项目报告；原邮件保留。
- 报告覆盖起因、目标与约束、过程、证据支持的因果链、结果、风险、总结与反思，既便于人类阅读，也便于 Agent 稳定提取。
- 新 Agent 的接入提示词和实际工具白名单都包含上述协作规则与邮箱能力。

## 规格符合度

结论：通过，无未修复发现。

- 归档会创建长期报告，再次归档更新同一条记忆，不制造重复项。
- 模型输出不合格、不可用或超时时会回退到本地结构化报告；报告正文经过禁止字样和必备章节检查。
- 邮箱侧栏明确解释协作中转与归档作用；归档前确认框说明报告内容、原邮件保留和再次归档行为。
- Agent 规则包含主题复用、记忆补充检索、上下文简报、里程碑回复和归档报告边界。

## 工程质量

结论：通过，无未修复发现。

- 归档逻辑集中在邮箱核心层，REST 路由只等待核心结果；现有同步调用均已迁移为异步。
- 归档报告通过 `source + sourceId` 找回并更新，且重新关联原主题。
- 使用普通中文“项目归档 / 邮箱归档报告”标签，确保标签规范化后仍保留，同时避免误触发旧项目日志的自动完成逻辑。
- 修复标签清洗的 ASCII 字符判断；中文标签现在会正常保留，纯数字和纯符号仍会被过滤。
- smoke 回归覆盖创建、结构、禁止字样、双重可读性、主题关联、再次归档刷新和 Agent 接入规则。

## 运行风险

结论：通过，无阻断项。

- 归档报告写入失败时不会先移动邮件，用户可重试；界面整理期间阻止重复触发。
- 恢复邮件到收件箱不会删除已生成报告，界面对此有明确说明；新进展再次归档会刷新原报告。
- 归档输入上限为 60000 字符，模型调用设置 60 秒超时并提供无模型降级路径。
- 未覆盖：真实第三方大模型对极长、多语言邮件的生成质量；结构校验与本地报告保证功能仍可用。

## 验证证据

- `node scripts/verify.mjs`：最终通过；包括类型检查、生产构建、Agent 接入、22/22 记忆评测、检索、MCP、Loop、邮箱、LLM、自动化和 L3 审计。中间新增断言发现命名空间标签会被过滤、纯中文标签会被 ASCII 规则误删，两项均修复后重新通过。
- 隔离 Playwright QA：1440、768、375 三种视口无横向溢出；5 个 Agent 头像均为 36×36，内部 Logo 为 28×28 且 `object-fit: contain`。
- 归档确认、取消操作通过；控制台错误 0、失败请求 0、4xx/5xx 响应 0。
- `pnpm --config.verify-deps-before-run=false release:check`：通过；doctor 为 0 failure，性能、完整 smoke、启动器与备份恢复检查均通过。
