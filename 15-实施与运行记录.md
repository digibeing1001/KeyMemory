# KeyMemory 认证与体验修复实施记录

日期：2026-08-06

## 根因

1. 注册密码框缺少 `new-password` 语义，浏览器密码管理器会按已有密码查找，出现“找不到密码”一类提示。
2. 首次安装与已有账户共用静态登录页，无法自动选择正确入口。
3. 无 body 的 POST 被通用请求封装错误标记为 JSON，Fastify 在路由前返回 400，影响登出及多个日常操作。
4. 手机侧栏的桌面内联层级覆盖了移动端层级，打开后遮罩会挡住侧栏操作。
5. 历史重构保留了“项目整理”菜单，却移除了页面挂载；页面模式白名单也漏掉多个合法值。

## 已实施

- 新增公开认证状态接口；首次安装自动注册，已有账户自动登录。
- 注册加入确认密码、显示密码、最小长度、字段 autocomplete 和本地化错误。
- 服务端统一姓名与邮箱输入、大小写无关登录、稳定认证错误码和安全错误响应。
- 修复空 POST 请求头与移动侧栏层级。
- 恢复项目整理页面，并补全页面模式偏好的合法值。
- 新增认证回归测试并纳入 server 测试命令。

## 验证与回滚

- 完整仓库验证和真实浏览器认证旅程均通过。
- 本次数据库结构没有变化，不需要数据迁移。
- 如需回滚，可单独回退本次提交；现有用户、记忆和 session 表结构不受影响。

---

# 记忆邮箱协作中转与主题归档实施记录

日期：2026-08-06

## 原因

1. 已发现 Agent 只显示横向头像，侧栏宽度不足时 Logo 会被压缩，用户也不能直接看到名称。
2. 邮箱虽然已有主题、回复和记忆秘书整理，但“归档”只移动文件夹，没有把项目全过程沉淀为可复用报告。
3. Agent 接入规则要求先读邮箱，却没有要求在开工前把主题与记忆整理成上下文简报，也没有定义归档报告标准。
4. Hermes 工具白名单缺少邮箱能力，规则即使写入也可能无法执行。

## 已实施

- Agent 区域改为竖排 Logo、名称和连接状态，固定头像尺寸并保持图像比例。
- 归档前向用户解释影响；归档时根据全部邮件和关联记忆创建长期项目报告，再次归档刷新原报告。
- 报告生成规则加入证据边界、稳定章节、自然语言和 Agent 可提取结构；不合格模型输出自动回退。
- 重写所有新 Agent 的接入规则：先读主题、补充检索、发送上下文简报、持续回复里程碑、复用主题并遵守归档沉淀规则。
- 补齐 Hermes 邮箱工具白名单和接入验收中的建主题工具。
- 归档报告使用独立标签，避免与旧项目日志自动化发生语义冲突。
- 修复标签清洗对中文文字的误判，避免中文标签写入后无声消失。

## 验证与回滚

- 完整仓库验证、发布检查与隔离浏览器验收通过；本次没有数据库结构迁移。
- 回滚代码不会删除已生成的归档报告或原邮件；这些报告仍是普通长期记忆，可由用户保留或手动归档。
- 生产进程需要重启后才会加载新的服务端归档逻辑和 Agent 接入提示词。
