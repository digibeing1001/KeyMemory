# KeyMemory Agent 查询指南

## 五层记忆定义与查询规则

### flash（闪念）

**定义**：临时想法、灵感、未验证的假设、快速记录
**判别特征**：
- 含"灵感""想法""想到""如果""试试""也许"等词
- 内容简短且未结构化
- 无明确行动项

**Agent 查询规则**：
- 不主动查询闪念层
- 仅在用户明确要求"找找我的想法/灵感"时检索
- 闪念可通过 `relates_to` 关联被命中，但不应作为首选答案来源

---

### short（短期）

**定义**：近期任务、待办事项、有明确时效的行动项
**判别特征**：
- 含"待办""本周""今天""明天""截止日期"等时效词
- 有明确的行动主体和交付物
- 完成后可归档或删除

**Agent 查询规则**：
- 当用户询问"今天要做什么""本周任务""有什么待办"时检索
- 优先检索 `status:待办` 或 `type:task` 标记的记忆
- 结合 `short` 层 + `flash` 层（含待办标记）

---

### long（长期）

**定义**：可复用的知识、方法论、学习笔记、经验总结
**判别特征**：
- 含"框架""原理""概念""学习""总结""方法论""最佳实践"等词
- 结构化内容（有章节、列表、代码示例）
- 长期有效，不因时间推移而失效

**Agent 查询规则**：
- **最高优先级**：当用户询问技术问题、寻求建议、需要背景知识时优先检索
- 检索范围：`long` 层 + `type:知识` 标记的记忆
- 结果按 `hit_count` 和 `confidence` 排序

---

### project（项目）

**定义**：特定项目的会议记录、决策、里程碑、进展汇报
**判别特征**：
- 含"项目""周会""会议纪要""决策""里程碑""评审"等词
- 归属于特定项目（通过 `project` 字段或 `project:xxx` 标签）
- 有明确的上下文和参与人

**Agent 查询规则**：
- 当用户**明确提及项目名称**时，优先检索该项目下的记忆
- 检索策略：
  1. `layer = 'project' AND project = '项目名'`
  2. `project:项目名` 标签的记忆（任何层级）
  3. 全层语义搜索，但优先返回标记了该项目的记忆

---

### entity（人事物）

**定义**：人物档案、组织信息、关键对象/工具的详细资料
**判别特征**：
- 含"职位""联系方式""偏好""风格""档案"等词
- 以特定人/组织/工具为中心
- 信息结构化（基本信息、特长、偏好等）

**Agent 查询规则**：
- 当用户**提及人名、组织名**时检索
- 检索策略：
  1. `layer = 'entity'` 且标题或内容匹配人名
  2. 通过 `memory_entities` 关联表查找该实体关联的所有记忆
  3. 返回实体档案 + 关联记忆列表

---

## 标签命名空间规范

Agent 应识别并利用以下命名空间标签进行精确查询：

| 命名空间 | 用途 | Agent 查询示例 |
|---------|------|---------------|
| `type:` | 内容类型 | `type:知识` `type:决策` `type:会议纪要` |
| `project:` | 项目归属 | `project:AI咨询` `project:前端架构` |
| `domain:` | 领域分类 | `domain:技术` `domain:商业` `domain:设计` |
| `status:` | 状态标记 | `status:待验证` `status:已确认` |
| `source:` | 信息来源 | `source:会议` `source:阅读` `source:对话` |

---

## 结构化关联类型

Agent 应理解并利用以下关联类型：

| 关联类型 | 含义 | 查询策略 |
|---------|------|---------|
| `part_of` | A 是 B 的一部分 | 查询项目时，同时返回 part_of 该项目的记忆 |
| `derived_from` | A 衍生自 B | 查询灵感/框架时，同时返回衍生出的记忆 |
| `relates_to` | A 与 B 相关 | 通用关联，扩大检索范围 |
| `supersedes` | A 替代 B | 返回最新版本，忽略被替代的旧版本 |
| `references` | A 引用 B | 查询学习笔记时，同时返回引用的原始资料 |

---

## 查询优先级总结

```
场景 1: 用户询问任务/待办
→ 查 short 层 + flash 层（status:待办）

场景 2: 用户询问知识/建议/怎么做
→ 先查 long 层（type:知识），再全层语义搜索
→ 结果按 long > project > entity 排序

场景 3: 用户提及项目名称
→ 查 project 层（该项目）+ 全层（project:该项目）

场景 4: 用户提及人名/组织名
→ 查 entity 层 + 关联记忆（通过 relations 表）

场景 5: 用户无明确意图
→ 全层语义搜索，按 hit_count + layer 权重排序

场景 6: 用户询问灵感/想法
→ 查 flash 层 + long 层（type:想法）
```

---

## 示例查询

### 示例 1
**用户**："AI咨询项目的认知框架是什么样的？"

**Agent 策略**：
1. 识别项目名："AI咨询"
2. 查询 `layer = 'project' AND project = 'AI咨询'`
3. 查询含 `project:AI咨询` 标签的 `long` 层记忆（`type:知识`）
4. 返回项目记忆 + 关联的长期知识

### 示例 2
**用户**："张三这周有什么安排？"

**Agent 策略**：
1. 识别人名："张三"
2. 查询 `layer = 'entity'` 且标题含"张三"
3. 通过 `memory_entities` 查找张三关联的所有记忆
4. 筛选 `short` 层（近期任务）和 `flash` 层（待办标记）
5. 返回张三的档案 + 近期关联任务

### 示例 3
**用户**："帮我找一下关于 React Server Components 的学习资料"

**Agent 策略**：
1. 关键词："React Server Components"
2. 优先查询 `long` 层（`type:知识` 或 `type:学习`）
3. 语义搜索全层，但提升 `long` 层权重
4. 返回学习笔记、总结、教程类记忆
